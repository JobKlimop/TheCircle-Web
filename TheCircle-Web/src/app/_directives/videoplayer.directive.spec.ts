import { VideoplayerDirective } from './videoplayer.directive';

describe('VideoplayerDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoplayerDirective();
    expect(directive).toBeTruthy();
  });
});
